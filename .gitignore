node_modules/
.workbuddy/
*.zip
.DS_Store
dist/
coverage/

# === secrets / local env (never commit) ===
env.txt
.env
.env.local
.env.*.local
!.env.example
# temp diagnostic probes / trash (may contain secrets — never track)
_trash_hr_nm/
.p4_live.env
.p4_*.mjs

# vitest/vite cache dirs (local, relative to each package)
.vitest-tmp/

# Vercel local project metadata (never commit)
.vercel
