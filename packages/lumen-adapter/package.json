{
  "name": "@busos/lumen-adapter",
  "version": "1.0.0",
  "private": true,
  "description": "AI Business OS BUSOS-P5-01 — Lumen (光砚) generation port + adapters. Real adapter maps the deployed Lumen HTTP API; the app layer depends only on the LumenPort interface (D018).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --no-cache",
    "verify": "npm run typecheck && npm run test"
  },
  "devDependencies": {
    "@types/node": "22.10.2",
    "typescript": "5.6.3",
    "vitest": "2.1.8"
  }
}
