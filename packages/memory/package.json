{
  "name": "@busos/memory",
  "version": "1.0.0",
  "private": true,
  "description": "AI Business OS canonical governed Memory foundation (R2-H2-01). A typed, auditable intelligence layer over canonical BUSOS entities — provenance, lifecycle, idempotency. No LLM, no vector DB, no second database.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --no-cache",
    "verify": "npm run typecheck && npm run test"
  },
  "dependencies": {
    "@busos/contracts": "file:../contracts",
    "zod": "3.23.8"
  },
  "devDependencies": {
    "@types/node": "22.10.2",
    "typescript": "5.6.3",
    "vitest": "2.1.8"
  }
}
