{
  "name": "@busos/evaluation",
  "version": "1.0.0",
  "private": true,
  "description": "AI Business OS R2-H2-03 — Evaluation Foundation & Golden Set Bootstrap. Canonical EvaluationCaseV1 golden-set schema, deterministic judges (retrieval / memory / governance / generation), metrics, runner, regression gates and reporters. Tier-1 CI: offline, deterministic, no secret, no external API. LLM-as-a-judge = FOUNDATION READY only.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --no-cache",
    "verify": "npm run typecheck && npm run test",
    "eval": "vite-node scripts/run-eval.ts"
  },
  "dependencies": {
    "@busos/contracts": "file:../contracts",
    "@busos/golden-path": "file:../golden-path",
    "@busos/memory": "file:../memory",
    "zod": "3.23.8"
  },
  "devDependencies": {
    "@types/node": "22.10.2",
    "typescript": "5.6.3",
    "vitest": "2.1.8"
  }
}
